import { BodycolorDirective } from './bodycolor.directive';

describe('BodycolorDirective', () => {
  it('should create an instance', () => {
    const directive = new BodycolorDirective();
    expect(directive).toBeTruthy();
  });
});
